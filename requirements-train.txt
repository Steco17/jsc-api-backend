# This tested compatibility set retains the Transformers 4 API used by the
# training script and includes PEFT selective token training.
transformers==4.57.6
datasets==4.4.2
peft==0.18.1
accelerate==1.12.0
sentencepiece==0.2.2
sacrebleu==2.5.1
ctranslate2==4.8.1

# Colab supplies a CUDA-specific PyTorch wheel.  A bounded range avoids an
# unnecessary multi-gigabyte reinstall while excluding an unknown major API.
torch>=2.6,<3.0
